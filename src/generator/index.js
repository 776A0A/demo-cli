/* eslint-disable no-unused-vars */
module.exports = (answers) => {
    const {
        commitizen,
        changelog,
        eslint,
        prettier,
        editorConfig,
        husky,
        lintStaged,
    } = answers
}
